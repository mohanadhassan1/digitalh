export enum TOAST_TYPES {
  SUCCESS = 'success',
  WARN = 'warn',
  INFO = 'info',
  ERROR = 'error'
}

export enum LOGIN {
  EMAIL = "email",
  PASSWORD = "password",
}

export enum PRODUCT {
  TITLE = 'title',
  PRICE = 'price',
  DESCRIPTION = 'description',
  CATEGORY_ID = 'categoryId',
}